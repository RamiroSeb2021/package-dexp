% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Potencia_Efectos_Aleatorios.R
\name{encontrar_r_minimo}
\alias{encontrar_r_minimo}
\title{Encontrar el tamaño mínimo de réplica que logra la potencia objetivo}
\usage{
encontrar_r_minimo(
  t,
  rho,
  potencia_objetivo = 0.8,
  sigma2 = 1,
  alpha = 0.05,
  nsim = 1000,
  r_max = 50
)
}
\arguments{
\item{t}{Número de tratamientos \eqn{t}.}

\item{rho}{Razón de varianzas \eqn{\rho = \sigma_\tau^2 / \sigma^2}.}

\item{potencia_objetivo}{Potencia deseada (1 - β). (default: 0.8)}

\item{sigma2}{Varianza del error \eqn{\sigma^2}. (default: 1)}

\item{alpha}{Nivel de significancia. (default: 0.05)}

\item{nsim}{Número de simulaciones de Monte Carlo por valor de \eqn{r}. (default: 1000)}

\item{r_max}{Límite superior para la búsqueda de \eqn{r}. (default: 50)}
}
\value{
Lista con:
\describe{
\item{r_optimo}{Primer \eqn{r} con potencia ≥ \code{potencia_objetivo}.}
\item{potencia}{Potencia empírica alcanzada para \eqn{r_optimo}.}
\item{grafico}{Gráfico de la potencia simulada vs \eqn{r}.}
\item{tabla}{Data frame con los valores de \eqn{r} y sus respectivas potencias \eqn{1-\beta}.}
}
Devuelve \code{NULL} (y lanza \code{warning}) si no se alcanza la potencia deseada.
}
\description{
Incrementa el número de réplicas \eqn{r} desde 2 hasta \code{r_max},
llamando a \code{simular_potencia()} en cada paso, hasta alcanzar la potencia
deseada. Además, construye el gráfico de la curva de potencia y devuelve
la tabla de resultados.
}
\examples{
# Encontrar el tamaño mínimo de réplicas para rho = 0.4
resultado <- encontrar_r_minimo(t = 5, rho = 0.4, potencia_objetivo = 0.8)
resultado$r_optimo
resultado$potencia
resultado$grafico
resultado$tabla

}
